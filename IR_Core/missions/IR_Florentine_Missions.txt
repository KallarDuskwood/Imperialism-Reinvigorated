ir_lan_slot_1 = {
	slot = 1
	generic = no
	ai = yes
	
	### Becasue I want to use the images...
	potential_on_load = {
		has_dlc = "Emperor"
	}
	
	potential = {
		has_dlc = "Emperor"
		OR = {
			tag = TUS
			tag = LAN
		}
		NOT = { map_setup = map_setup_random }
	}
	
	has_country_shield = yes

	
	
	
}

ir_lan_slot_1 = {
	slot = 2
	generic = no
	ai = yes
	
	### Becasue I want to use the images...
	potential_on_load = {
		has_dlc = "Emperor"
	}
	
	potential = {
		has_dlc = "Emperor"
		OR = {
			tag = TUS
			tag = LAN
		}
		NOT = { map_setup = map_setup_random }
	}
	
	has_country_shield = yes

	
	
	
}